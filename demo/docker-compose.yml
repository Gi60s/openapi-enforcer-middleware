version: '3'

services:
  api:
    build: src
    restart: always
    env_file:
      - .env
    ports:
      - 3000:3000

  db:
    image: postgres:11
    restart: always
    env_file:
      - .env
    volumes:
      - ./sql:/var/lib/postgresql/data